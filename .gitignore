.DS_Store

# Local runtime state and secrets.
node_modules/
.env
.env.*
!.env.example
.railway/

# Paid products must be delivered after checkout, never from the public static site.
books/paid/

# Internal launch strategy is kept in the local workspace, not the public repository.
COMMUNITY_LAUNCH_PLAN.md
